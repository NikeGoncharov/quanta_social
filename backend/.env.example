# Copy to .env for local/production overrides.

# development | production
ENVIRONMENT=development

# SQLite (default resolves to backend/data/data.db). Override for a custom path.
# DATABASE_URL=sqlite+aiosqlite:///./data/data.db
# DATABASE_URL_SYNC=sqlite:///./data/data.db

# CORS origin of the SPA in local dev (prod is same-origin behind Caddy).
FRONTEND_URL=http://localhost:5173

# --- Auth (Phase 4) ---
# REQUIRED in production. Generate: python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=dev-insecure-change-me
ACCESS_TOKEN_TTL_MIN=30
REFRESH_TOKEN_TTL_DAYS=30

# Comma-separated allowlist of emails permitted to register. Empty = open (dev only).
ALLOWED_REGISTRATION_EMAILS=

# --- Deploy (Phase 6) ---
# Cloudflare Tunnel token (used by the cloudflared service in compose.yaml).
TUNNEL_TOKEN=
